# 技术原理

本文档说明 [`smg_fivestar.user.js`](./smg_fivestar.user.js)（油猴脚本）与 [`CONSOLE.md`](./CONSOLE.md)（Console 粘贴代码）的工作原理。两者当前均为 **v0.19**，共享同一套核心逻辑，差异仅在交付形态（见 §8）。

---

## 1. 背景：为什么看不了

看看新闻直播页（`live.kankanews.com/huikan`）是一个 Nuxt.js（Vue 2）单页应用，播放器为 xgplayer + HLS 插件。服务端对版权节目（如 F1）做两层拦截：

| 层级 | 手段 | 效果 |
|------|------|------|
| 页面层 | 节目数据 `is_shield=1`，频道带 `copyright_image` | 前端不初始化播放器，盖一层 `.image-mask` 版权遮罩图 |
| 接口层 | `channel/detail`、`program/detail` 返回的 `live_address` / `shift_address` 置空 | 即使绕过页面层，也拿不到流地址 |

2026-09 起，服务端把五星体育（channel_id=10）的 `live_address` **全部掏空**（连非版权节目也不给），旧版"从直播播放器 URL 偷 token"的路子彻底失效，这是 v0.18+ 重构的直接原因。

---

## 2. 总体流程

```
打开页面
  → 隐藏版权遮罩 + 改 Vue 数据（is_shield=0 等）+ 关 isCopyright 开关
  → Token 自举：找官方回放节目 → 解密 shift_address → 拿到 CDN token（§4）
  → 拦截 initPlayer：
      直播（play≠0）→ 裸 manifest + 看门狗保活（§6）
      回放（play=0）→ startTime 偏移 URL + 虚拟位置/寻道全套 hook（§5、§7）
```

---

## 3. 版权绕过（直播页）

纯前端数据篡改，共 4 步：

1. CSS 隐藏 `.image-mask` / `.video-tip` 遮罩层；
2. 改 Vue 组件数据：`is_shield=0`、`is_review=1`、`can_review=1`（节目对象、节目详情、播放中节目、节目列表全改），清空 `copyright_image`；
3. 置 `isCopyright=false`——网站用该计算属性做播放器初始化的门控，为真时 `initPlayer()` 直接销毁播放器；
4. 调用 `initPlayer()`（已被脚本覆写，见 §6、§7）。

附加清理：试看倒计时（`countdown` 置大数 / `startCountdown` 置空）、切页暂停（`pageVisibilityChange` 置空并重绑）、`liveTimer` 清除。

---

## 4. Token 自举（v0.18+ 核心）

### 4.1 为什么需要自举

CDN（字节 volc-stream）只认 token，不认节目是否版权。任意有效 token + 任意时间戳都能播。但 ch10 已无任何地址可偷，必须另找 token 来源。

### 4.2 发现：官方回放节目仍带地址

每天一条的《体育新闻》（`is_review=1`）的 `program/detail` 仍返回 `shift_address`（RSA 加密的流 URL）。用它解密出的 token 可用于全频道任意时间点。

### 4.3 API 签名

请求 `kapi.kankanews.com` 需签名，算法从页面 JS 逆向得到：

```
sign = md5(md5("k1=v1&k2=v2&...&" + "28c8edde3d61a0411511d3b1866f0636"))
```

参与字段 = 业务参数 + `platform=pc / version=2.42.23 / nonce(8位随机) / timestamp / Api-Version=v1`，按键名排序拼接。脚本内置精简 MD5 实现，不依赖外部库。

**关键坑**：签名字段必须放 **HTTP header**。同样的签名放 query 里，服务端返回 `code=4001` 且地址字段为空；放 header 里才返回 `code=1000` 带地址。服务端验签通过后，还按请求形状 gate 地址字段。

### 4.4 RSA 解密（即网站 E.a / webpack module 560）

`shift_address` 是分块 RSA 密文，解密流程与网站 `E.a` 完全一致：`atob` → 转大写 hex → 每 256 字符一切 → hex 转 b64 → JSEncrypt（页面自带库）+ 硬编码公钥解密 → 拼接。解密得明文流 URL，解析出 `token / volcSecret / volcTime / stream / exp`。

油猴脚本另带 module 560 的 monkey-patch：明文 `http` URL 直接透传，避免被 E.a 二次"解密"成空串。

### 4.5 Donor 队列与回翻

只有 `is_review=1` 的节目才带地址，且**当天未播出的没有地址**（如下午查当晚的体育新闻是空的）。队列按优先级组织：

```
当天节目单的 is_review=1 节目 → 硬编码历史 ID → 逐天回翻节目单（最多 7 天）
```

逐个调 `program/detail`，空地址则跳下一个。token 缓存，有效期按 `exp` 判断。

### 4.6 Token TTL 缩短（v0.19）

服务端签发的 token TTL 已从 ~12h 缩到**约 6 分钟**（实测）。旧逻辑"拿一次用半天"会导致播几分钟后 CDN 报 403 转圈。应对：有效期判定 margin 收紧到 2 分钟 + 直播看门狗主动刷新（§6）。

---

## 5. CDN 时间偏移（回放基础）

volc-stream 支持 `&startTime=UNIX_TIMESTAMP`，返回该时间点的 HLS 流。两种形态：

| URL 形态 | 返回 | 用途 |
|----------|------|------|
| 裸 manifest（无 startTime） | 原生滑动窗口（VERSION:3，每次 poll seq 递增） | 直播 |
| `startTime=` manifest | **固定 3 个分片**（~30s），同 startTime 永远返回相同分片 | 回放起点 / 寻道目标 |

回放的"30 秒冻结"即源于此：HLS.js 轮询同一 URL 永远拿到相同分片。解法见 §7。

---

## 6. 直播分支（v0.19）

- 用**裸 manifest**（`makeLive()`，无 startTime），原生滑动窗口，不挂任何改写 hook；
- 启动**看门狗**（30s tick）：
  - token 剩余有效期 < 2 分钟 → 重新自举 → `switchURL` 无缝切到新 token URL；
  - `video.error.code===4`（媒体错误）→ 最多 3 次重建恢复；
- 播放器 `isLive=true`，原生进度行为。

---

## 7. 回放分支

### 7.1 动态 startTime（防 30 秒冻结）

Hook HLS 的 `manifestLoader.load`：每次轮询把 `startTime` 改写为 `programStart + _virtualPos`，让每次 poll 拿到当前位置的新分片，实现无限续播。

### 7.2 虚拟位置追踪器

不依赖 `player.currentTime`（HLS 切源后会重置为 0），而用独立变量 `_virtualPos`：播放中每 500ms 按墙钟推进；首次寻道前每 1s 从 `currentTime` 同步一次。进度条/时间显示通过 `offsetCurrentTime` 暴露，并用 `Object.defineProperty` 锁定 getter——HLS 插件在重建 MediaSource 时会把它重置为 -1，不锁则进度条乱跳。

### 7.3 寻道（进度条拖动）

覆写 `player.seek(time)`，**不调原 seek**（新 manifest 的媒体时间轴从 0 开始，原 seek 的节目内秒数无意义）：

1. 立即更新 `_virtualPos`（UI 即时响应）；
2. 150ms 防抖（拖动过程只切一次）；
3. generation 计数器丢弃过期请求；
4. Promise 队列串行化切源，防止多次异步 `switchURL` 竞态；
5. `player.switchURL(url, {seamless:false, currentTime:0})` 做完整切源（清旧 MSE 缓冲 → 加载新 manifest → 从相对 0 起播），恢复暂停/播放状态。

---

## 8. 两个文件的差异

| | `smg_fivestar.user.js`（油猴） | `CONSOLE.md`（Console） |
|---|---|---|
| 运行方式 | 打开页面自动执行 | F12 粘贴，回车执行，刷新后需重贴 |
| Vue 探测 | 4 路径（`.huikan.__vue__` / `#__nuxt` 遍历 / 元素扫描 / 全局扫描，Safari/Stay 兼容） | 仅 `.huikan.__vue__` |
| 移动端 | `replaceState` 就地修复被服务端写坏的 URL | 无 |
| API 拦截器 | XHR/fetch 钩子，接口返回后自动重打补丁 | 无（一次性执行） |
| SPA 切频道 | 监听 URL 变化自动重打补丁 | 需重新粘贴 |
| 倒计时/切页暂停 | 常驻清理（定时器 + 事件重绑） | 一次性清理 |
| 模式 | 直播 + 回放一体 | A（直播+回放）/ B（仅直播备用，已半失效） |
| 核心逻辑 | 与 Console 版一致（token 自举 / 看门狗 / 回放 hook） | 与油猴版一致 |

---

## 9. 已知限制

- **CDN 验 IP**：token 绑定 `user_ip`，海外直连会被拒，需回国链路（见 issue #5）。
- **Donor 依赖**：若服务端把旧节目 `shift_address` 也掏空，自举即失效，需找新路子。
- **Token TTL 短**：约 6 分钟，靠看门狗续期；长时间挂播依赖看门狗正常工作。
- **Playwright 不可测解码**：MPEG-TS 音频在无头 Chromium 报 `MEDIA_ERR_SRC_NOT_SUPPORTED`，端到端只能验证到 manifest/分片层。
